import styled from 'styled-components/native';
import { BoldText } from './BoldText';
import { colors } from '@UI';

export const BigTitleText = styled(BoldText)({
    fontSize: 32,
    color: colors.blue,
});
